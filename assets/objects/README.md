# Comic environment objects

`crates-v1.png` is the original transparent health crate, ammunition crate and
explosive barrel atlas, generated with the built-in imagegen tool.
Exact generation prompt: `prompt-v1.txt`.
Runtime alpha-bound cropping preserves the source PNG and places visible feet
at the object's simulation position. Crates render at their 24×22 pickup bounds.
