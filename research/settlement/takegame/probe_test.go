package main

import "testing"

// Includes the complete 128-combination positive matrix and fresh-signature
// negative cases. No fixtures are written unless the executable's export flag
// is explicitly supplied.
func TestGameStateMatrixAndAdversarialSpends(t *testing.T) {
	runProbe()
}
